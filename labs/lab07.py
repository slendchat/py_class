from lab07_module import *

age_in_days = calculate_age_in_days()
print(f"Вы прожили {age_in_days} дней.")

weekday_name = get_weekday_for_date()
print(f"День недели для указанной даты: {weekday_name}.")

fall_time = calculate_fall_time()
print(f"Время падения объекта с указанной высоты: {fall_time:.2f} секунд.")